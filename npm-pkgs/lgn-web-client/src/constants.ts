/**
 * Key to the l10n orchestrator (localization related store set) context.
 *
 * Used in the `L10n` component.
 */
export const l10nOrchestratorContextKey = Symbol.for(
  "l10n-orchestrator-context-key"
);
