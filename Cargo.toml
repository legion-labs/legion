[workspace]
members = [
    "client/*",
    "server/*",
    "lib/*",
    "plugin/*",
    "proto/*",
    "examples/*",
    "test/*",
    "compiler/*",
    "build/monorepo",
    "build/workspace-hack",
]
exclude = ["lib/vite-plugins"]
