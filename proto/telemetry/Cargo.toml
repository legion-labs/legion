[package]
name = "legion-telemetry-proto"
version = "0.1.0"
edition = "2018"
license = "MIT OR Apache-2.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tonic = "0.5"
prost = "0.8"
tokio = { version = "1.12", features = ["macros", "rt-multi-thread"] }
base64 = "0.13"

[build-dependencies]
tonic-build = "0.5"
