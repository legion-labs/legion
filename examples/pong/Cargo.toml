[package]
name = "pong"
version = "0.1.0"
authors = ["Legion Labs <devs@legionlabs.com>"]
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
# Legion engine
runtime-srv = { path = "../../server/runtime-srv", version = "0.1.0" }
