[package]
name = "legion-analytics"
version = "0.1.0"
edition = "2018"
license = "MIT OR Apache-2.0"

[dependencies]
legion-telemetry = { path = "../../lib/telemetry" }
sqlx = { version = "0.5", features = [
    "sqlite",
    "any",
    "mysql",
    "runtime-tokio-native-tls",
] }
anyhow = "1.0"
prost = "0.8"
transit = { path = "../../lib/transit" }
legion-telemetry-proto = { path = "../../proto/telemetry", version = "0.1.0" }
