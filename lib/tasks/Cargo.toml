[package]
name = "lgn-tasks"
version = "0.1.0"
edition = "2021"
description = "A task executor for the Legion Engine"
license = "MIT OR Apache-2.0"

[dependencies]
bevy_tasks = { git = "https://github.com/legion-labs/bevy", default-features = false }
