[package]
name = "lgn-ecs"
version = "0.1.0"
edition = "2021"
description = "Legion's entity component system"
homepage = "https://legionengine.com"
repository = "https://github.com/legion-labs/legion"
license = "MIT OR Apache-2.0"
keywords = ["ecs", "game", "legion"]
categories = ["game-engines", "data-structures"]

[dependencies]
bevy_ecs = { version = "0.5.0", default-features = false }
bevy_ecs_macros = { version = "0.5.0", default-features = false }
