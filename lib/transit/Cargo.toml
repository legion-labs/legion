[package]
name = "transit"
version = "0.1.0"
edition = "2018"
license = "MIT OR Apache-2.0"

[dependencies]
derive-transit = { path = "derive-transit" }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"

[dev-dependencies]
memoffset = "0.6"
