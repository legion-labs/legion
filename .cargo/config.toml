[cargo-new]
name = "Legion Labs"
email = "devs@legionlabs.com"
vcs = "none"

[alias]
cg = "build --features=run-codegen"
codegen = "build --features=run-codegen"

[build]
# 
# make rustdoc treat warnings as errors.
rustdocflags = ["-Dwarnings"]

[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"
