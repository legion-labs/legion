[cargo-new]
name = "Legion Labs"
email = "devs@legionlabs.com"
vcs = "none"

[alias]
mr = "run -q --package lgn-monorepo --bin lgn-monorepo --"
mr-clippy = "run -q --package lgn-monorepo --bin lgn-monorepo -- clippy"
mrc = "mr-clippy"

[build]
# 
# make rustdoc treat warnings as errors.
rustdocflags = ["-Dwarnings"]

[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"
