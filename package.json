{
  "private": true,
  "workspaces": [
    "client/editor-svelte/frontend",
    "client/analytics-web/frontend",
    "proto/editor"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "check:all": "yarn workspaces run check:all",
    "test": "yarn workspaces run test"
  }
}
