export type Resolution = { width: number; height: number };

/** Structurally similar to a Resolution but used in different contexts */
export type Size = Resolution;

export type Position = { x: number; y: number };
