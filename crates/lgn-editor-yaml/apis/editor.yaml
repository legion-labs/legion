openapi: 3.1.0
info:
  title: "Streaming API"
  description: "The OpenAPI specification for the Legion Labs editor."
  version: 1.0.0
paths:
  /v1/spaces/{space-id}/workspace/{workspace-id}/session/editor:
    parameters:
      - $ref: "../../lgn-governance/apis/space.yaml#/components/parameters/space_id"
      - $ref: "../../lgn-governance/apis/workspace.yaml#/components/parameters/workspace_id"
    post:
      description: Undo transaction request.
      operationId: UndoTransactionRequest
      responses:
        "200":
          description: Ok.
          